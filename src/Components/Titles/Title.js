import React from 'react'
import { TitleText } from '../../Style/elments'

function Title({name}) {
    return (
        <TitleText>
            {name}
        </TitleText>
    )
}

export default Title
